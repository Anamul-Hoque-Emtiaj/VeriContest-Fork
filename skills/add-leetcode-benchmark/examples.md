**Sorting**: `lc75`, `lc88`. Partition and in-place merge patterns with clear index invariants.

**Two Pointers**: `lc26`, `lc167`. Monotonicity or partition invariants on sorted arrays.

**Sliding Window**: `lc209`, `lc643`. Fixed and variable window sums with explicit bounds on `l` and `r`.

**Prefix Sum**: `lc238`, `lc303`. Range queries as differences of prefixes; `lc303` adds a small `Design` wrapper.

**Binary Search**: `lc35`, `lc69`, `lc74`. Standard templates on indices, monotone predicates, and 2D sorted structure.

**Greedy**: `lc55`, `lc122`, `lc134`. Local choice plus exchange-argument style reasoning; `lc55` is also tagged DP, but the greedy proof is the main one here.

**Dynamic Programming**: `lc53`, `lc70`. Small iterative DP recurrences with clear optimal substructure.

**Stack**: `lc946`. Explicit push/pop state simulation.

**Queue / Monotonic Queue**: `lc1438`. Sliding min/max deques.

**Bit Manipulation**: `lc136`, `lc338`. XOR parity and bit-count DP over a small integer domain.

**Bitmask**: `lc473`. Subset DP and bitmask loops.

**Math**: `lc7`, `lc9`, `lc50`. Integer bounds, palindrome checks, and fast power.

**Number Theory**: `lc2413`, `lc2427`, `lc3658`. Divisibility and gcd-style arguments.

**Geometry**: `lc223`, `lc836`, `lc883`. Coordinate tests and rectangle overlap with straightforward predicates.

**Enumeration**: `lc1291`, `lc1534`. Explicit bounded candidate generation over a finite search space.

**Counting**: `lc169`, `lc229`, `lc1160`. Frequency maps and tallies tied to array bounds.

**Simulation**: `lc54`, `lc59`, `lc289`. Matrix walks with explicit direction and state updates.

**Design**: `lc303`. Immutable prefix-sum wrapper with a simple representation invariant.

**Game Theory**: `lc292`, `lc486`, `lc3222`. Turn-based winning predicates with compact state reasoning.

**Brainteaser**: `lc292`, `lc319`. Compact closed-form reasoning.

**Recursion**: `lc390`. Direct recursive structure with an easy-to-state recurrence.

**Backtracking**: `lc473`. Explicit choice points and pruning.

**Matrix**: `lc54`, `lc59`, `lc74`. Spirals, layers, and 2D binary search—row/col bounds recur in specs.
